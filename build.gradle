plugins {
    id 'java'
    id 'application'
}

group = 'com.brainterminator'
version = '1.0-SNAPSHOT'

application {
    mainClass = 'com.brainterminator.sudoku.solver.App'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}